# Privileged helper boundary

This directory intentionally contains no executable helper. Any future privileged operation must use an explicit allowlisted API and approval flow; the main AI process must not gain administrator shell access.

